services:
  - type: web
    name: ai-gym-backend
    env: python
    rootDir: backend
    pythonVersion: 3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn control_server:app --host 0.0.0.0 --port 10000
